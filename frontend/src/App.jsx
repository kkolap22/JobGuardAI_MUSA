import { useEffect, useMemo, useState } from "react";
import { Link, NavLink, Navigate, Route, Routes, useLocation, useNavigate, useSearchParams } from "react-router-dom";
import { ArrowRight, Check, CheckCircle2, ChevronRight, CircleAlert, Clock3, CloudUpload, ExternalLink, History as HistoryIcon, Link2, LoaderCircle, LockKeyhole, LogIn, Menu, Radar, Search, Shield, ShieldCheck, Sparkles, TriangleAlert, UserRound, X } from "lucide-react";
import { api } from "./api";
import heroImage from "../assets/jobguard-hero.png";
import leadPhoto from "../assets/raghav-lead.jpg";
import devPhoto from "../assets/developer-member.jpg";
import riteshPhoto from "../assets/ritesh-ai.png";
import harshPhoto from "../assets/harsh-research.jpg";

const green = "text-[#08ad50]";
const button = "inline-flex items-center justify-center gap-2 rounded-xl bg-[#078e42] px-5 py-3 text-sm font-bold text-white shadow-lg shadow-[#08ad50]/20 transition hover:-translate-y-0.5 hover:bg-[#06783a] disabled:cursor-not-allowed disabled:opacity-60";

function Header() {
  const location = useLocation();
  const navigate = useNavigate();
  const [open, setOpen] = useState(false);
  const user = api.getUser();
  const links = [["/", "Home"], ["/scan", "Scan"], ["/history", "History"], ["/about", "About Us"]];
  return <header className="sticky top-0 z-30 border-b border-[#e4e7ec]/80 bg-white/85 backdrop-blur-xl">
    <div className="flex h-[76px] w-full items-center justify-between px-6 lg:px-12">
      <Link to="/" className="font-display text-2xl font-bold tracking-tight">Job<span className={green}>Guard+</span></Link>
      <nav className="hidden h-full items-center gap-6 md:flex">{links.map(([to, label]) => <NavLink key={to} to={to} end className={({ isActive }) => `group relative flex h-[76px] min-w-[70px] items-center justify-center px-2 text-sm font-bold whitespace-nowrap transition-colors ${isActive ? green : "text-slate-600 hover:text-[#08ad50]"}`}>{({ isActive }) => <><span>{label}</span><div className="absolute bottom-0 left-1/2 -translate-x-1/2 w-12 overflow-hidden"><span className={`block h-[2.5px] w-full rounded-full bg-[#08ad50] transition-transform duration-300 origin-right ${isActive ? "scale-x-100" : "scale-x-0 group-hover:scale-x-100"}`} /></div></>}</NavLink>)}</nav>
      <div className="hidden items-center gap-4 md:flex">{user ? <><span className="flex items-center gap-2 text-sm font-bold text-[#07843e]"><UserRound size={17} />Hi, {user.name?.split(" ")[0] || "Account"}</span><button onClick={() => { api.logout(); }} className="rounded-xl border border-red-200 bg-red-50 px-4 py-2 text-sm font-bold text-red-600 shadow-sm transition hover:border-red-600 hover:bg-red-600 hover:text-white">Logout</button></> : <><Link to="/login" className="rounded-lg border border-[#e4e7ec] px-4 py-2 text-sm font-bold text-slate-700 hover:border-[#08ad50] hover:text-[#08ad50]">Login</Link><Link to="/signup" className={button}>Sign Up</Link></>}</div>
      <button aria-label="Toggle menu" onClick={() => setOpen(!open)} className="rounded-lg p-2 text-slate-700 md:hidden">{open ? <X /> : <Menu />}</button>
    </div>
    {open && <div className="border-t border-slate-100 bg-white px-5 py-4 md:hidden">{links.map(([to, label]) => <NavLink key={to} onClick={() => setOpen(false)} to={to} className="block border-b border-slate-100 py-3 text-sm font-bold text-slate-700">{label}</NavLink>)}<Link to={user ? "/scan" : "/login"} onClick={() => setOpen(false)} className="mt-4 block text-sm font-bold text-[#08ad50]">{user ? "Open workspace" : "Login / Sign up"}</Link></div>}
  </header>;
}

function Footer() { return <footer className="flex flex-col justify-between gap-2 border-t border-slate-200 px-5 py-7 text-xs text-slate-400 sm:flex-row lg:px-10"><span>JobGuard+ © 2026</span><span>Safer Jobs. Smarter Decisions.</span></footer>; }
function Layout({ children }) { return <><Header /><main>{children}</main><Footer /></>; }
function Tag({ icon: Icon = ShieldCheck, children }) { return <span className="inline-flex items-center gap-2 rounded-full border border-[#c9efd6] bg-[#eafff1] px-3 py-2 text-[11px] font-extrabold tracking-[.14em] text-[#07843e]"><Icon size={15} />{children}</span>; }
function Protected({ children }) { return api.isAuthenticated() ? children : <Navigate to="/login" replace />; }

function Home() { return <Layout><section className="hero-grid border-b border-slate-100/80"><div className="w-full items-center justify-between gap-8 px-6 py-16 lg:grid lg:min-h-[640px] lg:grid-cols-[0.9fr_1.2fr] lg:px-12 lg:py-10"><div className="max-w-2xl"><Tag>AI-POWERED JOB SAFETY</Tag><h1 className="mt-7 font-display text-6xl font-bold leading-[.94] tracking-[-.06em] sm:text-7xl lg:text-[82px]">Find Jobs,<br /><span className={green}>Not Scams.</span></h1><p className="mt-6 max-w-xl text-base leading-7 text-slate-500">Verify job postings, detect scams and apply with confidence. JobGuard+ helps you stay one step ahead in your career journey.</p><div className="mt-8 flex flex-wrap gap-3"><Link to="/scan" className={button}>Check a Job Post <ArrowRight size={17} /></Link><a href="#system" className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-5 py-3 text-sm font-bold text-slate-700 hover:border-[#08ad50] hover:text-[#08ad50]"><Sparkles size={17} /> Watch Demo</a></div></div><div className="flex items-center justify-center overflow-hidden lg:justify-end"><img src={heroImage} alt="JobGuard job safety illustration" className="h-auto w-full max-w-xl object-contain lg:max-w-3xl xl:max-w-[800px]" /></div></div></section><section id="system" className="section-band"><SectionIntro tag="WHAT HAPPENS BEHIND THE SCENES" title="How JobGuard Checks a Job" text="When you scan a job link, JobGuard opens it safely, watches what happens on the page and turns the collected evidence into a clear risk report." /><div className="mx-auto grid max-w-7xl gap-4 px-5 sm:grid-cols-2 lg:grid-cols-4 lg:px-10">{[[Link2,"Job Link","Paste the job posting URL and start a scan."],[Radar,"FastAPI","The backend creates and manages your scan."],[Shield,"Sandbox","Every investigation runs in an isolated environment."],[Search,"Playwright","A real browser observes the job site safely."]].map(([Icon, title, text], i) => <InfoCard key={title} Icon={Icon} title={`${i + 1}. ${title}`} text={text} />)}</div></section><section className="px-5 py-20 lg:px-10"><SectionIntro tag="HOW IT WORKS" title="Your Safety, Our Priority" text="Three simple steps to verify any job posting." /><div className="mx-auto grid max-w-7xl gap-4 md:grid-cols-3">{[[Link2,"Paste Job Link","Add the URL of the job posting you want to investigate."],[Search,"We Investigate","Our system checks website behaviour, redirects, forms and suspicious signals."],[ShieldCheck,"Get Your Report","See the risk level, findings and safety guidance instantly."]].map(([Icon, title, text], i) => <InfoCard key={title} Icon={Icon} title={`0${i + 1}  ${title}`} text={text} />)}</div></section><Cta /></Layout>; }
function SectionIntro({ tag, title, text }) { return <div className="mx-auto mb-10 max-w-2xl px-5 text-center"><span className="text-[11px] font-extrabold tracking-[.14em] text-[#08ad50]">{tag}</span><h2 className="mt-3 font-display text-4xl font-bold tracking-tight sm:text-5xl">{title}</h2><p className="mt-3 text-sm leading-6 text-slate-500">{text}</p></div>; }
function InfoCard({ Icon, title, text }) { return <article className="rounded-2xl border border-slate-200 bg-white p-6 transition hover:-translate-y-1 hover:border-[#a8dfb8] hover:shadow-xl"><div className="grid h-12 w-12 place-items-center rounded-xl bg-[#eafff1] text-[#08ad50]"><Icon size={23} /></div><h3 className="mt-6 font-display text-lg font-bold">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p></article>; }
function Cta() { return <section className="mx-5 mb-16 flex flex-col justify-between gap-6 rounded-3xl bg-[linear-gradient(120deg,#067b3b,#0cbd5b)] p-8 text-white sm:p-12 lg:mx-10 lg:flex-row lg:items-center"><div><h2 className="font-display text-4xl font-bold tracking-tight">Don't apply blindly.<br />Check it first.</h2><p className="mt-3 text-sm text-[#d9f8e5]">Investigate job postings and make safer career decisions.</p></div><Link to="/scan" className="inline-flex w-fit items-center gap-2 rounded-xl bg-white px-5 py-3 text-sm font-extrabold text-[#08783b]">Start a Job Scan <ArrowRight size={17} /></Link></section>; }

function AuthPage({ mode }) { const isLogin = mode === "login"; const Icon = isLogin ? LogIn : ShieldCheck; const navigate = useNavigate(); const [form, setForm] = useState({ name: "", email: "", password: "" }); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const submit = async (e) => { e.preventDefault(); setError(""); setLoading(true); try { if (isLogin) await api.login(form.email, form.password); else await api.register(form.name, form.email, form.password); navigate("/scan"); } catch (err) { setError(err.message); } finally { setLoading(false); } }; return <div className="auth-bg flex min-h-[calc(100vh-153px)] items-center justify-center px-5 py-16"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-slate-200 bg-white p-8 shadow-2xl shadow-slate-900/10 sm:p-10"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-[#eafff1] text-[#08ad50]"><Icon size={28} /></div><div className="mt-6 text-center"><Tag>{isLogin ? "WELCOME BACK" : "JOIN JOBGUARD+"}</Tag><h1 className="mt-4 font-display text-3xl font-bold">{isLogin ? "Sign in to your account" : "Create your account"}</h1><p className="mt-2 text-sm text-slate-500">{isLogin ? "Continue checking jobs with confidence." : "Start making safer career decisions."}</p></div><div className="mt-8 space-y-4">{!isLogin && <Field label="Full name" value={form.name} onChange={(v) => setForm({ ...form, name: v })} placeholder="Your name" />}{<Field label="Email" type="email" value={form.email} onChange={(v) => setForm({ ...form, email: v })} placeholder="you@example.com" />}{<Field label="Password" type="password" value={form.password} onChange={(v) => setForm({ ...form, password: v })} placeholder="••••••••" />}</div>{error && <div className="mt-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}<button disabled={loading} className={`${button} mt-6 w-full`}>{loading ? <LoaderCircle className="animate-spin" size={17} /> : <Icon size={17} />}{loading ? "Processing..." : isLogin ? "Login" : "Create account"}</button><p className="mt-6 text-center text-sm text-slate-500">{isLogin ? "New to JobGuard+? " : "Already have an account? "}<Link className="font-bold text-[#08ad50]" to={isLogin ? "/signup" : "/login"}>{isLogin ? "Sign up" : "Login"}</Link></p></form></div>; }
function Field({ label, value, onChange, type = "text", placeholder }) { return <label className="block text-sm font-bold text-slate-700">{label}<input required type={type} value={value} onChange={(e) => onChange(e.target.value)} placeholder={placeholder} className="mt-2 h-12 w-full rounded-xl border border-slate-200 px-4 font-normal outline-none transition placeholder:text-slate-400 focus:border-[#08ad50] focus:ring-4 focus:ring-[#08ad50]/10" /></label>; }

function Scan() { const [params] = useSearchParams(); const [url, setUrl] = useState(""); const [scan, setScan] = useState(null); const [report, setReport] = useState(null); const [findings, setFindings] = useState([]); const [loading, setLoading] = useState(false); const [progress, setProgress] = useState(0); const [error, setError] = useState(""); const loadResult = async (id) => { const [s, r, f] = await Promise.all([api.getScan(id), api.getReport(id).catch(() => ({ data: null })), api.getScanFindings(id).catch(() => ({ data: [] }))]); setScan(s.data); setReport(r.data); setFindings(f.data || []); }; useEffect(() => { const id = params.get("scanId"); if (id) loadResult(id).catch((e) => setError(e.message)); }, [params]); const submit = async (e) => { e.preventDefault(); setError(""); setScan(null); if (!/^https?:\/\//i.test(url)) { setError("Valid HTTP ya HTTPS URL enter karein."); return; } setLoading(true); setProgress(15); try { const created = await api.createScan(url); const id = created.data.scanId; let done = false; for (let attempt = 0; attempt < 60 && !done; attempt++) { await new Promise((resolve) => setTimeout(resolve, 2000)); setProgress(Math.min(92, 18 + attempt * 4)); const current = await api.getScan(id); setScan(current.data); done = ["COMPLETED", "FAILED", "STOPPED"].includes(current.data.status); } if (!done) throw new Error("Scan timed out. History se result check karein."); await loadResult(id); setProgress(100); } catch (err) { setError(err.message); } finally { setLoading(false); } }; const score = scan?.riskScore ?? report?.riskScore ?? 0; const level = (scan?.riskLevel || report?.riskLevel || "EVALUATING").toUpperCase(); const risk = level === "HIGH" || level === "CRITICAL" ? "danger" : level === "MEDIUM" ? "warn" : "safe"; return <Layout><section className="mx-auto max-w-4xl px-5 py-16 text-center"><Tag>AI-POWERED JOB SAFETY</Tag><h1 className="mt-6 font-display text-5xl font-bold tracking-tight sm:text-7xl">Check a Job<br /><span className={green}>Before You Apply.</span></h1><p className="mx-auto mt-5 max-w-2xl text-base leading-7 text-slate-500">Paste a job posting URL and JobGuard+ will investigate its behaviour, redirects and suspicious signals.</p></section><section className="mx-auto max-w-3xl px-5 pb-20"><form onSubmit={submit} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-2xl shadow-slate-900/10 sm:p-10"><div className="flex items-center gap-4"><div className="grid h-12 w-12 place-items-center rounded-xl bg-[#eafff1] text-[#08ad50]"><Search /></div><div><h2 className="font-display text-2xl font-bold">Scan Job Posting</h2><p className="text-sm text-slate-500">Enter the job posting URL below to begin.</p></div></div><label className="mt-8 block text-sm font-bold">Job Posting URL<div className="mt-2 flex h-14 items-center gap-3 rounded-xl border border-slate-200 px-4 focus-within:border-[#08ad50] focus-within:ring-4 focus-within:ring-[#08ad50]/10"><Link2 className="text-slate-400" size={19} /><input value={url} onChange={(e) => setUrl(e.target.value)} type="url" placeholder="https://example.com/job/software-developer" className="w-full outline-none" /></div></label>{error && <div className="mt-4 flex items-center gap-2 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700"><CircleAlert size={17} />{error}</div>}<button disabled={loading} className={`${button} mt-6 h-14 w-full text-base`}>{loading ? <><LoaderCircle className="animate-spin" />Analyzing... {progress}%</> : <><Radar size={19} />Scan Job <ArrowRight size={18} /></>}</button>{loading && <div className="mt-5 h-2 overflow-hidden rounded-full bg-[#dff3e6]"><div className="h-full rounded-full bg-[#08ad50] transition-all" style={{ width: `${progress}%` }} /></div>}<p className="mt-5 flex items-center justify-center gap-2 text-xs text-slate-500"><LockKeyhole size={14} /> Securely processed in an isolated sandbox.</p></form>{scan && <Result scan={scan} report={report} findings={findings} risk={risk} score={score} level={level} />}</section></Layout>; }
function Result({ scan, report, findings, risk, score, level }) { const all = findings.length ? findings : report?.findings || []; const failed = scan.status === "FAILED"; return <article className="mt-8 rounded-3xl border border-slate-200 bg-white p-6 shadow-xl sm:p-9"><div className="flex items-start justify-between gap-4"><div><span className="text-[11px] font-extrabold tracking-[.14em] text-[#08ad50]">SCAN RESULT</span><h2 className="mt-2 font-display text-2xl font-bold">Job Safety Report</h2><p className="mt-1 break-all text-xs text-slate-500">{scan.jobUrl}</p></div><div className={`grid h-12 w-12 place-items-center rounded-xl ${failed || risk === "danger" ? "bg-red-50 text-red-600" : risk === "warn" ? "bg-amber-50 text-amber-600" : "bg-[#eafff1] text-[#08ad50]"}`}>{failed || risk === "danger" ? <TriangleAlert /> : <ShieldCheck />}</div></div><div className={`mt-7 flex items-center gap-6 rounded-2xl p-6 ${failed || risk === "danger" ? "bg-red-50" : risk === "warn" ? "bg-amber-50" : "bg-[#f4fff8]"}`}><strong className={`font-display text-6xl ${failed || risk === "danger" ? "text-red-600" : risk === "warn" ? "text-amber-600" : "text-[#08ad50]"}`}>{failed ? "—" : score}</strong><div><b className="text-sm">{failed ? "SCAN FAILED" : `${level} RISK`}</b><p className="mt-1 text-sm text-slate-500">{failed ? (scan.error || "The scanner could not complete this job check.") : report?.summary || (scan.status === "COMPLETED" ? "Analysis complete." : `Scan status: ${scan.status}`)}</p></div></div><div className="mt-8"><h3 className="font-display text-xl font-bold">Findings & Detected Signals</h3>{all.length ? all.map((finding, i) => <div key={i} className="flex gap-3 border-b border-slate-100 py-4 text-sm"><CircleAlert className="shrink-0 text-amber-500" size={18} /><span><b>{(finding.type || "Finding").replaceAll("_", " ")}: </b>{finding.evidence || finding.explanation || finding.description || "Signal detected"}</span></div>) : failed ? <div className="mt-4 flex items-center gap-2 text-sm text-red-700"><TriangleAlert size={18} />No safety conclusion was produced because the scan failed.</div> : <div className="mt-4 flex items-center gap-2 text-sm text-[#07843e]"><CheckCircle2 size={18} />No high-risk scam signals detected.</div>}</div></article>; }

function History() { const [scans, setScans] = useState([]); const [query, setQuery] = useState(""); const [error, setError] = useState(""); useEffect(() => { api.getUserScans().then((res) => setScans(res.data || [])).catch((e) => setError(e.message)); }, []); const filtered = useMemo(() => scans.filter((s) => s.jobUrl?.toLowerCase().includes(query.toLowerCase())), [scans, query]); return <Layout><section className="hero-grid px-5 py-16 text-center lg:px-10"><Tag icon={HistoryIcon}>YOUR SCAN ACTIVITY</Tag><h1 className="mt-6 font-display text-6xl font-bold tracking-tight">Scan<br /><span className={green}>History.</span></h1><p className="mx-auto mt-5 max-w-xl text-sm leading-7 text-slate-500">Review detected risks and revisit your previous job investigations whenever you need them.</p></section><section className="mx-auto max-w-6xl px-5 pb-20 lg:px-10"><div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><span className="text-[11px] font-extrabold tracking-[.14em] text-[#08ad50]">HISTORY</span><h2 className="mt-2 font-display text-4xl font-bold">Recent Scans</h2></div><span className="inline-flex w-fit items-center gap-2 rounded-xl bg-[#eafff1] px-3 py-2 text-xs font-bold text-[#07843e]"><HistoryIcon size={15} />{scans.length} Total Scans</span></div><div className="mb-5 flex items-center gap-3 rounded-2xl border border-slate-200 bg-white p-3"><Search className="text-slate-400" size={19} /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search scanned jobs..." className="w-full outline-none" /></div>{error && <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</p>}<div className="space-y-3">{filtered.map((scan) => <HistoryRow key={scan.scanId} scan={scan} />)}</div>{!filtered.length && !error && <div className="rounded-2xl border border-dashed border-slate-300 p-12 text-center"><HistoryIcon className="mx-auto text-[#08ad50]" size={36} /><h3 className="mt-4 font-display text-xl font-bold">No scan records found</h3><Link to="/scan" className={`${button} mt-5`}>Start a Job Scan <ArrowRight size={17} /></Link></div>}</section></Layout>; }
function HistoryRow({ scan }) { const level = (scan.riskLevel || (scan.riskScore > 65 ? "HIGH" : scan.riskScore > 30 ? "MEDIUM" : "LOW")).toUpperCase(); const danger = ["HIGH", "CRITICAL"].includes(level); const warn = level === "MEDIUM"; return <article className="grid gap-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg md:grid-cols-[1.5fr_.8fr_.65fr_auto] md:items-center"><div className="flex min-w-0 items-center gap-3"><div className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-[#eafff1] text-[#08ad50]"><ExternalLink size={20} /></div><div className="min-w-0"><h3 className="truncate font-display font-bold">{new URL(scan.jobUrl).hostname}</h3><p className="truncate text-xs text-slate-500">{scan.jobUrl}</p></div></div><div className="text-xs text-slate-500"><span className="block uppercase tracking-wider">Scanned</span><span className="font-semibold text-slate-700">{scan.createdAt ? new Date(scan.createdAt).toLocaleString() : "Recently"}</span></div><span className={`w-fit rounded-full px-3 py-2 text-xs font-bold ${danger ? "bg-red-50 text-red-700" : warn ? "bg-amber-50 text-amber-700" : "bg-[#eafff1] text-[#08783b]"}`}>{danger ? "High Risk" : warn ? "Suspicious" : level === "QUEUED" ? "Queued" : "Safe"}</span><Link to={`/scan?scanId=${encodeURIComponent(scan.scanId)}`} className="inline-flex w-fit items-center gap-1 text-sm font-bold text-[#07843e]">View <ChevronRight size={16} /></Link></article>; }

function About() { return <Layout><section className="hero-grid px-5 py-20 text-center lg:px-10"><Tag icon={UserRound}>THE PEOPLE BEHIND JOBGUARD+</Tag><h1 className="mt-6 font-display text-6xl font-bold tracking-tight">Meet Our<br /><span className={green}>Team.</span></h1><p className="mx-auto mt-5 max-w-xl text-sm leading-7 text-slate-500">JobGuard+ is built by people passionate about technology, cybersecurity and safer digital experiences for job seekers.</p></section><section className="px-5 py-20 lg:px-10"><SectionIntro tag="OUR TEAM" title="People Behind the Project" text="A focused team making job searching safer, smarter and more reliable." /><div className="mx-auto grid max-w-6xl gap-5 sm:grid-cols-2 lg:grid-cols-4">{[["Frontend & UI","Raghav Pokar","Crafting responsive user interfaces, design systems and seamless user experience."],["Backend & Cybersecurity","Ketan Kolapkar","Building secure API architecture, database workflows, testing and code reviews."],["Full Stack Developer","Ritesh Bhosale","Building end-to-end frontend and backend systems, integrating Gemini AI, APIs, databases, and intelligent job-risk analysis features."],["Research & UI","Harsh Limbani","Investigating job scam techniques, threat intelligence and refining UI workflows."]].map(([role, name, desc]) => <article key={name} className="overflow-hidden rounded-2xl border border-slate-200 bg-white">{name === "Raghav Pokar" ? <img src={leadPhoto} alt={name} className="h-72 w-full object-cover object-top" /> : name === "Ketan Kollapkar" ? <img src={devPhoto} alt={name} className="h-72 w-full object-cover object-top" /> : name === "Ritesh Bhosale" ? <img src={riteshPhoto} alt={name} className="h-72 w-full object-cover object-top" /> : <img src={harshPhoto} alt={name} className="h-72 w-full object-cover object-top" />}<div className="p-5"><span className="text-[10px] font-extrabold uppercase tracking-widest text-[#08ad50]">{role}</span><h3 className="mt-2 font-display text-xl font-bold">{name}</h3><p className="mt-2 text-xs leading-5 text-slate-500">{desc}</p></div></article>)}</div></section><section className="px-5 pb-20 lg:px-10"><div className="mx-auto grid max-w-6xl items-center gap-8 rounded-3xl border border-[#c9efd6] bg-[#f4fff8] p-8 sm:p-12 md:grid-cols-[.35fr_1fr]"><div className="grid h-24 w-24 place-items-center rounded-3xl bg-[#eafff1] text-[#08ad50]"><Shield size={44} /></div><div><span className="text-[11px] font-extrabold tracking-widest text-[#08ad50]">WHY WE BUILT JOBGUARD+</span><h2 className="mt-2 font-display text-3xl font-bold">Making job searching a little safer.</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-500">Our goal is to combine technology, security and simple explanations into one easy-to-use platform that helps people identify suspicious opportunities before making risky decisions.</p></div></div></section><Cta /></Layout>; }

export default function App() { return <Routes><Route path="/" element={<Home />} /><Route path="/login" element={<Layout><AuthPage mode="login" /></Layout>} /><Route path="/signup" element={<Layout><AuthPage mode="signup" /></Layout>} /><Route path="/scan" element={<Protected><Scan /></Protected>} /><Route path="/history" element={<Protected><History /></Protected>} /><Route path="/about" element={<About />} /><Route path="*" element={<Navigate to="/" replace />} /></Routes>; }
